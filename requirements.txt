requests>=2.28
beautifulsoup4>=4.11
pyyaml>=6.0

# Optional — needed only for 'forge compile --format pdf' and 'forge run --format pdf'
# If not installed, Prompt Forge falls back to HTML (which you can print-to-PDF).
reportlab>=4.0; python_version >= "3.8"
